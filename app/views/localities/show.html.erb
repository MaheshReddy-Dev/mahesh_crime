<%= render "shared/remote_modal", title: "Locality Information" do %>

<p>locality name: <%=@locality.locality%></p>
reported crimes in the locality:
<% @locality.crimes.each do |crime|%>
  <ul>
    <li><%=crime.crime_type%></li>
  </ul>
<% end %>

<h2>
  <%=link_to 'Edit locality', edit_locality_path(@locality),data: { turbo_frame: "remote_modal" }, class: "btn btn-primary" %>
  <h2><%= link_to 'Back', crimes_path, class: "btn btn-secondary" %></h2>
</h2>
<% end %>